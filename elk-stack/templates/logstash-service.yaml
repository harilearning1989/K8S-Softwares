apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: elk-stack
spec:
  type: NodePort
  selector:
    app: logstash
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500