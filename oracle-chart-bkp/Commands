helm install oracle-db ./oracle-chart -n hari-practiced

kubectl get svc -n hari-practiced

Connect from DBeaver
Use these settings:
Driver: Oracle
Host: 127.0.0.1
Port: 31521
SID / Service Name: XEPDB1
User: system
Password: dudkrish1A
👉 In DBeaver, choose Connection type → Service name and enter XEPDB1.

CREATE USER practiced_user IDENTIFIED BY "dudkrish1A";

GRANT CREATE SESSION TO practiced_user;

GRANT DBA TO practiced_user;


CREATE TABLE employees (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR2(100) NOT NULL,
  department VARCHAR2(100),
  salary NUMBER(10,2),
  hire_date DATE
);

INSERT INTO employees (name, department, salary, hire_date)
VALUES ('Alice', 'Finance', 70000, DATE '2024-05-01');

SELECT * FROM employees;