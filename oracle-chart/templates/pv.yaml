apiVersion: v1
kind: PersistentVolume
metadata:
  name: oracle-pv
spec:
  capacity:
    storage: {{ .Values.oracle.storageSize }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.oracle.storagePath }}