apiVersion: v1
kind: Service
metadata:
  name: oracle
  namespace: hari-practiced
spec:
  selector:
    app: oracle
  ports:
    - port: 1521
      targetPort: 1521
      nodePort: {{ .Values.oracle.nodePort }}
  type: NodePort