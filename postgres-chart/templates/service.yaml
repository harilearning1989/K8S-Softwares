apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: hari-practiced
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: {{ .Values.postgres.nodePort }}
  type: NodePort
