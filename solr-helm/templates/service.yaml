apiVersion: v1
kind: Service
metadata:
  name: solr
spec:
  type: {{ .Values.service.type }}
  selector:
    app: solr
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8983
      nodePort: {{ .Values.service.nodePort }}